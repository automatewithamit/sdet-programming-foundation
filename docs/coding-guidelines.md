Java Coding Guidelines

1. Use PascalCase for class names
   Example: EvenOddChecker

2. Use camelCase for variables
   Example: userName

3. One class per problem

4. Use meaningful variable names

Avoid:
a, b, x, temp