package com.sdet.foundation.week1.oops;

public class ObjectOrientedProgramming {
    // OOPs Concepts:
    // 1. Encapsulation: Wrapping data and methods into a single unit (class).
    // 2. Inheritance: Deriving new classes from existing ones, promoting code reuse.
    // 3. Polymorphism: Ability of a variable, function, or object to take on multiple forms. It allows methods to do different things based on the object it is acting upon.
    // 4. Abstraction: Hiding complex implementation details and showing only the necessary features of an object. It allows you to focus on what an object does instead of how it does it.
    public static void main(String[] args) {
        // You can implement the OOPs concepts here by creating classes and objects to demonstrate encapsulation, inheritance, polymorphism, and abstraction.
    //Before OOPs, programming was primarily procedural,
        // where the focus was on writing procedures or routines to operate on data.
        // OOPs introduced a new way of thinking about programming by organizing code around objects that represent real-world entities, making it easier to manage and maintain complex software systems.
   // for example, you can create a class called "Car" that encapsulates properties like "make", "model", and "year",
        // and methods like "startEngine()" and "stopEngine()".
        // You can then create subclasses/child classes like "ElectricCar" and "GasolineCar" that inherit from the "Car" class, demonstrating inheritance.
        // Polymorphism can be shown by overriding the "startEngine()" method in both subclasses to provide different implementations. Abstraction can be demonstrated by creating an interface called "Vehicle" that defines the methods that all vehicles must implement, allowing you to focus on what a vehicle does rather than how it does it.

        //Pillars of OOPs:
        //1. Encapsulation: The process of bundling data/properties (variables) and methods/behaviors (functions) that operate on the data into a single unit (class) and restricting access to some of the object's components. This is achieved through access modifiers (private, protected, public) and getter/setter methods.
        //2. Inheritance: The mechanism by which one class (child class) can inherit properties and behaviors (fields and methods) from another class (parent class). This promotes code reuse and establishes a natural hierarchical relationship between classes.
        //3. Polymorphism: The ability of a variable, function, or object to take on multiple forms. It allows methods to do different things based on the object it is acting upon. Polymorphism can be achieved through method overloading (compile-time polymorphism) and method overriding (runtime polymorphism).
        //4. Abstraction: The concept of hiding the complex implementation details and showing only the necessary features of an object. It allows you to focus on what an object does rather than how it does it. Abstraction can be achieved through abstract classes and interfaces.



        // Example of OOPs concepts in Java:
        // Car myCar = new Car("Toyota", "Camry", 2020);
        // myCar.startEngine();
        //  myCar.stopEngine();

    }
}
